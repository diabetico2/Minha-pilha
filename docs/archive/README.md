# Arquivos históricos

`legacy-css/` preserva quatro estilos da versão anterior que já não eram carregados pelo site. Os estilos ativos ficam em `assets/css/`. Não inclua esses arquivos antigos no HTML: isso pode sobrepor o visual atual.
